from objects.vector3 import Vector3

class Sphere:
    def __init__(self, radius, position, color):
        self.radius = radius
        self.position = position
        self.color = color
        self.object_type = 1